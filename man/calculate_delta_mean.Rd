% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/month_deltas.R
\name{calculate_delta_mean}
\alias{calculate_delta_mean}
\title{Calculate Mean Month-to-Month Changes Across All Years}
\usage{
calculate_delta_mean(sea_delta = NULL)
}
\arguments{
\item{sea_delta}{A data frame returned by calculate_month_deltas().
If NULL, calculates from built-in data.}
}
\value{
A tibble with columns: Mes_num (1-11), Profunditat, delta_mean
(average delta across all years).
}
\description{
Computes the average temperature change between consecutive months
across all years in the dataset, for each month and depth.
}
\examples{
# Calculate from built-in data
mean_deltas <- calculate_delta_mean()
head(mean_deltas)

# Or provide your own deltas
deltas <- calculate_month_deltas()
mean_deltas <- calculate_delta_mean(deltas)

}
